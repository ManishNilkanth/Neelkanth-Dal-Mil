[build]
  command = "npm run build"
  publish = "dist"

# React Router routes (e.g. /admin/login) are not real files.
# Serve index.html so the client router can handle them.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
